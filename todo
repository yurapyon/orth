no envs because its intended for games
  where you have alot of control in zig to load different code units
  if it was more of a general purpose language envs make more sense

repl

// hardware
interlace stacks ie return/restore
ret res ret res __ res __ res __ __
